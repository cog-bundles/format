---
cog_bundle_version: 3
name: format
version: 0.2.0
docker:
  image: cogcmd/format
  tag: 0.2.0
commands:
  table:
    executable: cog-command
    rules: [ 'allow' ]
templates:
  preformatted:
    slack: '```{{ body }}```'
    hipchat: '<pre>{{ body }}</pre>'
